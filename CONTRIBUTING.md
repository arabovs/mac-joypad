# Contributing

macOS 14+ and Xcode Command Line Tools (`swift`, `clang++`).

```bash
./build.sh
open /Applications/Joypad.app
```

Do not commit `vendor/`, `dist/`, `.build/`, or `Signing/`. The first build clones Karabiner VirtualHIDDevice headers into `vendor/`.
